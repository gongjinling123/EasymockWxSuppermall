<!--components/w-tab.wxml-->
<view class="tab">
  <block wx:for="{{title}}" wx:key="index">
    <view class="tab-item {{currentIndex==index?'active':''}}"
          bind:tap="handleTap"
          data-index="{{index}}">
     <text>{{item}}</text>
    </view>
  </block>
</view>
 